window.es6Example.comprehension = {};

window.es6Example.comprehension.code = [
    'let arr = [1, 2, 3, 4, 5];',
    'let squared = [for (x of arr) x * x];',
    '',
    'console.log(squared);',
    ''
].join('\n');

window.es6Example.comprehension.display = 'Array Comprehension';
