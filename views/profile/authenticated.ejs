    <div class="content" class="clearfix">
      <div class="userphoto"><img src= "<%= user.avatar_url %>" alt="default avatar"></div>
      <div class="user-details">
        <h1><%= user.name %></h1>
        <section class="bio">
          @<%= user.login %>
        </section>
        <!--<% include starredFiddles %>-->
        <% include recentFiddles %>
        <% if (startedFiddles.length > 0) { %>
          <% include starredFiddles %>
        <% } %>
    </div>
    </div><!-- @end #content -->
